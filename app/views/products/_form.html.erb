<%= form_for(product) do |f| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :cost %>
    <%= f.text_field :cost %>
  </div>

  <div class="field">
    <%= f.label :collection %>
    <%= f.number_field :collection %>
  </div>

  <div class="field">
    <%= f.label :items %>
    <%= f.number_field :items %>
  </div>

  <div class="field">
    <%= f.label :contents %>
    <%= f.text_area :contents %>
  </div>

  <div class="field">
    <%= f.label :perishables %>
    <%= f.check_box :perishables %>
  </div>

  <div class="field">
    <%= f.label :non_perishables %>
    <%= f.check_box :non_perishables %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
